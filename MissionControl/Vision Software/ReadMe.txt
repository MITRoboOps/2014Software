Import desired program (saved in a folder named with the program number) into Sherlock BOA.

Programs Currently in Folder:
1 - TakeImages <Takes images and uploads them to the FTP server when the command send. is received. The terminating ASCII character is . >